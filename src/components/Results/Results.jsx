// import React from 'react';
// import './Results.css'; // Import your CSS file

// const Results = ({ results }) => {
//   return (
//     <div className="results-container">
//       {results.length > 0 && (
//         results.map((movie) => (
//           <div className="movie-card" key={movie.id}>
//             <img src={movie.image} alt={movie.title} /> {/* Assuming the API provides an image property */}
//             <h2>{movie.title}</h2>
//             <p>{movie.description.slice(0, 100)}...</p>
//           </div>
//         ))
//       )}
//     </div>
//   );
// };

// export default Results;
